<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FORMCV.dinamicpage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>

        <Label Text="{Binding MainContentProperty1}" TextColor="Blue" BackgroundColor="LightPink" Grid.Row="1" Grid.Column="0" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
        <Label Text= "{Binding MainContentProperty2}" Grid.Row="1" Grid.Column="1" TextColor="Blue" BackgroundColor="LightPink" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
        <Label Text= "{Binding MainContentProperty3} " Grid.Row="1" Grid.Column="2" Grid.RowSpan="2" TextColor="Blue" BackgroundColor="LightPink" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
        <Label Text= "{Binding MainContentProperty4} " Grid.Row="1" Grid.Column="3" Grid.RowSpan="2" TextColor="Blue" BackgroundColor="LightPink" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
         
    
    </Grid>
</ContentPage>